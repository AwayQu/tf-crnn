//
//  ImageExtension.swift
//  vision_text_detection
//
//  Created by MacDev on 2018/4/9.
//  Copyright © 2018年 MacDevHuang. All rights reserved.
//

import Cocoa
import CoreGraphics
extension NSImage {
//
//    /// Returns a image that fills in newSize
//    func resizedImage(newSize: CGSize) ->  NSImage {
//        // Guard newSize is different
//        guard self.size != newSize else { return self }
//
//
//
//        UIGraphicsBeginImageContextWithOptions(newSize, false, 0.0);
//        self.drawInRect(CGRectMake(0, 0, newSize.width, newSize.height))
//        let newImage: NSImage = UIGraphicsGetImageFromCurrentImageContext()
//        UIGraphicsEndImageContext()
//        return newImage
//    }
//
//    /// Returns a resized image that fits in rectSize, keeping it's aspect ratio
//    /// Note that the new image size is not rectSize, but within it.
//    func resizedImageWithinRect(rectSize: CGSize) -> NSImage {
//        let widthFactor = size.width / rectSize.width
//        let heightFactor = size.height / rectSize.height
//
//        var resizeFactor = widthFactor
//        if size.height > size.width {
//            resizeFactor = heightFactor
//        }
//
//        let newSize = CGSizeMake(size.width/resizeFactor, size.height/resizeFactor)
//        let resized = resizedImage(newSize)
//        return resized
//    }
    
}
